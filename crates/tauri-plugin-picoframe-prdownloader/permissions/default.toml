"$schema" = "schemas/schema.json"

[default]
description = "Allows the prdownloader plugin's commands (version, rapid browsing, download)."
permissions = [
  "allow-prd-version",
  "allow-prd-repos",
  "allow-prd-versions",
  "allow-prd-download",
]
