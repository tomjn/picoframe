{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "worker",
  "description": "Grants the worker plugin's default commands to the main window.",
  "windows": ["main"],
  "permissions": ["picoframe-worker:default"]
}
