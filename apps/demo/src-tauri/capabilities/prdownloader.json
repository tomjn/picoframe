{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "prdownloader",
  "description": "Grants the prdownloader plugin's default commands to the main window.",
  "windows": ["main"],
  "permissions": ["picoframe-prdownloader:default"]
}
