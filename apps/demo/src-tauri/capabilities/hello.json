{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hello",
  "description": "Grants the hello plugin's default commands to the main window.",
  "windows": ["main"],
  "permissions": ["picoframe-hello:default"]
}
